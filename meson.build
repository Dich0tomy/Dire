project(
	'Dire', 'cpp',
	version : '0.1.0',
	meson_version : '>= 1.2.0',
	license: 'mit',
	default_options: { 'default_library': 'static' }
)

# Contains all the subprojects - lib, app, tests
subdir('src')
